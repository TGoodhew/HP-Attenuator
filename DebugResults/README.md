# DebugResults

Scratch output from harness runs — result CSVs, debug captures, hardware-run logs.

**Everything in this folder except this README is git-ignored.** Put run artifacts here
instead of the repo root so they never need individual `.gitignore` entries. The test
harness writes its CSV here by default (`--out` overrides the path).
